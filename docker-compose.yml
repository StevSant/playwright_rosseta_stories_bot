version: '3'
services:
  oliver:
    image: rosseta-playwright-image

    env_file:
      - .env_oliver
    restart: always

  evelyn:
    image: rosseta-playwright-image
    env_file:
      - .env_evelyn
    restart: always
